# 📼 Инструкция по добавлению контента в игру "Подборщик Фильмов"

## Оглавление
1. [Как добавить новые фильмы](#как-добавить-новые-фильмы)
2. [Как добавить новые запросы посетителей](#как-добавить-новые-запросы-посетителей)
3. [Структура данных](#структура-данных)
4. [Примеры](#примеры)

---

## Как добавить новые фильмы

### Шаг 1: Откройте файл игры
Откройте файл `movie-picker-game.html` в текстовом редакторе.

### Шаг 2: Найдите базу данных фильмов
Найдите в коде секцию `MOVIES_DATABASE`. Она начинается примерно так:

```javascript
const MOVIES_DATABASE = [
    // Боевики
    { id: 1, title: "Матрица", year: 1999, genre: "action", director: "Вачовски", rating: 8.7, format: "DVD" },
    // ... остальные фильмы
];
```

### Шаг 3: Добавьте новый фильм
Добавьте новый объект фильма в конец массива перед закрывающей скобкой `]`.

**Обязательные поля:**
- `id` (число) - уникальный идентификатор (используйте следующий свободный номер)
- `title` (строка) - название фильма
- `year` (число) - год выпуска
- `genre` (строка) - жанр (см. список допустимых жанров ниже)
- `director` (строка) - имя режиссёра
- `rating` (число с точкой) - рейтинг от 0.0 до 10.0
- `format` (строка) - формат носителя: `"DVD"` или `"VHS"`

**Допустимые жанры:**
- `"action"` - Боевик (красный цвет)
- `"comedy"` - Комедия (жёлтый цвет)
- `"drama"` - Драма (синий цвет)
- `"horror"` - Хоррор (тёмно-серый цвет)
- `"scifi"` - Фантастика (фиолетовый цвет)
- `"romance"` - Мелодрама (розовый цвет)

### Пример добавления фильма:

```javascript
const MOVIES_DATABASE = [
    // ... существующие фильмы ...
    { id: 48, title: "Касабланка", year: 1942, genre: "romance", director: "Майкл Кёртиц", rating: 8.5, format: "VHS" },
    
    // Ваш новый фильм:
    { id: 49, title: "Интерстеллар", year: 2014, genre: "scifi", director: "Кристофер Нолан", rating: 8.6, format: "DVD" },
];
```

### ⚠️ Важно:
- Не забудьте поставить запятую после предыдущего фильма
- ID должен быть уникальным
- Используйте только допустимые жанры из списка выше
- Формат может быть только `"DVD"` или `"VHS"`
- Рейтинг указывайте с точкой, а не запятой (8.5, а не 8,5)

---

## Как добавить новые запросы посетителей

### Шаг 1: Найдите базу запросов
Найдите в коде секцию `CUSTOMER_REQUESTS`:

```javascript
const CUSTOMER_REQUESTS = [
    {
        id: 1,
        text: "Хочу что-то с взрывами и экшеном!",
        filters: { genre: "action" }
    },
    // ... остальные запросы
];
```

### Шаг 2: Добавьте новый запрос
Добавьте новый объект запроса в конец массива.

**Структура запроса:**
- `id` (число) - уникальный идентификатор
- `text` (строка) - текст запроса посетителя (то, что он говорит)
- `filters` (объект) - набор фильтров для отбора подходящих фильмов

### Доступные фильтры:

| Фильтр | Тип | Описание | Пример |
|--------|-----|----------|--------|
| `genre` | строка | Жанр фильма | `"action"`, `"comedy"`, `"drama"`, `"horror"`, `"scifi"`, `"romance"` |
| `yearMin` | число | Минимальный год выпуска | `1990` |
| `yearMax` | число | Максимальный год выпуска | `1999` |
| `format` | строка | Формат носителя | `"DVD"` или `"VHS"` |
| `ratingMin` | число | Минимальный рейтинг | `8.0` |
| `director` | строка | Имя режиссёра (точное совпадение) | `"Фрэнк Дарабонт"` |

### Примеры запросов:

#### Простой запрос (один фильтр):
```javascript
{
    id: 16,
    text: "Покажи мне триллер!",
    filters: { genre: "horror" }
}
```

#### Запрос с несколькими фильтрами:
```javascript
{
    id: 17,
    text: "Хочу боевик 2000-х годов!",
    filters: { 
        genre: "action",
        yearMin: 2000,
        yearMax: 2009
    }
}
```

#### Сложный запрос:
```javascript
{
    id: 18,
    text: "Дай мне топовую комедию 90-х на кассете!",
    filters: { 
        genre: "comedy",
        yearMin: 1990,
        yearMax: 1999,
        format: "VHS",
        ratingMin: 7.5
    }
}
```

#### Запрос по режиссёру:
```javascript
{
    id: 19,
    text: "Есть что-нибудь от Стивена Спилберга?",
    filters: { 
        director: "Стивен Спилберг"
    }
}
```

### ⚠️ Важно:
- ID должен быть уникальным
- Текст запроса должен быть понятным и интересным для игрока
- Фильтры должны соответствовать атрибутам фильмов в базе
- Можно комбинировать любое количество фильтров
- Имя режиссёра должно точно совпадать с тем, что указано в базе фильмов

---

## Структура данных

### Полная структура фильма:
```javascript
{
    id: 99,                          // Уникальный номер
    title: "Название фильма",        // Название
    year: 2020,                      // Год выпуска (число)
    genre: "action",                 // Жанр (строка из списка)
    director: "Имя Режиссёра",      // Режиссёр
    rating: 8.5,                     // Рейтинг (число с точкой)
    format: "DVD"                    // Формат: DVD или VHS
}
```

### Полная структура запроса:
```javascript
{
    id: 20,                                    // Уникальный номер
    text: "Текст запроса посетителя",        // Что говорит посетитель
    filters: {                                 // Фильтры отбора
        genre: "action",                      // Опционально: жанр
        yearMin: 1990,                        // Опционально: год от
        yearMax: 1999,                        // Опционально: год до
        format: "VHS",                        // Опционально: формат
        ratingMin: 8.0,                       // Опционально: мин. рейтинг
        director: "Имя Режиссёра"            // Опционально: режиссёр
    }
}
```

---

## Примеры

### Пример 1: Добавление классики
```javascript
// Добавляем фильм
{ 
    id: 50, 
    title: "Унесённые ветром", 
    year: 1939, 
    genre: "romance", 
    director: "Виктор Флеминг", 
    rating: 8.2, 
    format: "VHS" 
},

// Добавляем запрос для него
{
    id: 20,
    text: "Хочу классику довоенного кино!",
    filters: { 
        yearMax: 1945,
        genre: "romance"
    }
}
```

### Пример 2: Добавление современного фильма
```javascript
// Добавляем фильм
{ 
    id: 51, 
    title: "Начало", 
    year: 2010, 
    genre: "scifi", 
    director: "Кристофер Нолан", 
    rating: 8.8, 
    format: "DVD" 
},

// Добавляем запрос
{
    id: 21,
    text: "Покажи современную фантастику от Нолана!",
    filters: { 
        director: "Кристофер Нолан",
        genre: "scifi",
        yearMin: 2000
    }
}
```

### Пример 3: Добавление нескольких фильмов одного жанра
```javascript
// Добавляем несколько боевиков
{ id: 52, title: "Рэмбо", year: 1982, genre: "action", director: "Тед Котчефф", rating: 7.7, format: "VHS" },
{ id: 53, title: "Коммандо", year: 1985, genre: "action", director: "Марк Л. Лестер", rating: 6.7, format: "VHS" },
{ id: 54, title: "Хищник", year: 1987, genre: "action", director: "Джон Мактирнан", rating: 7.8, format: "VHS" },

// Добавляем запрос, который их покроет
{
    id: 22,
    text: "Дай мне олдскульный боевик 80-х с мускулистыми героями!",
    filters: { 
        genre: "action",
        yearMin: 1980,
        yearMax: 1989,
        format: "VHS"
    }
}
```

---

## Советы по балансировке игры

1. **Разнообразие жанров**: Старайтесь добавлять фильмы разных жанров равномерно
2. **Эпохи**: Включайте фильмы из разных десятилетий
3. **Форматы**: Балансируйте количество DVD и VHS
4. **Рейтинги**: Добавляйте как шедевры (8+), так и средние фильмы (6-7)
5. **Сложность запросов**: 
   - Простые запросы (1 фильтр) - легко угадать
   - Средние запросы (2-3 фильтра) - нужно подумать
   - Сложные запросы (4+ фильтра) - настоящий вызов

---

## Проверка изменений

После добавления новых фильмов и запросов:

1. ✅ Убедитесь, что все ID уникальные
2. ✅ Проверьте, что нет лишних или пропущенных запятых
3. ✅ Откройте файл в браузере
4. ✅ Протестируйте игру с новыми данными
5. ✅ Проверьте, что новые запросы корректно фильтруют фильмы

---

## Часто встречающиеся ошибки

❌ **Ошибка**: Игра не запускается после изменений
✅ **Решение**: Проверьте консоль браузера (F12) на наличие синтаксических ошибок

❌ **Ошибка**: Посетитель всегда недоволен
✅ **Решение**: Убедитесь, что в базе есть фильмы, соответствующие фильтрам запроса

❌ **Ошибка**: Неправильный цвет корешка
✅ **Решение**: Проверьте, что указали правильный жанр из списка допустимых

❌ **Ошибка**: Дублирующиеся ID
✅ **Решение**: Каждый ID должен быть уникальным, используйте следующий свободный номер

---

## Расширенные возможности

### Добавление нового жанра

Если вы хотите добавить новый жанр (например, "вестерн"), нужно:

1. Добавить цветовой класс в CSS:
```css
.genre-western { background: linear-gradient(to bottom, #8b7355 0%, #654321 100%); }
```

2. Добавить название жанра в объект `GENRE_NAMES`:
```javascript
const GENRE_NAMES = {
    action: "Боевик",
    comedy: "Комедия",
    drama: "Драма",
    horror: "Хоррор",
    scifi: "Фантастика",
    romance: "Мелодрама",
    western: "Вестерн"  // новый жанр
};
```

3. Добавить в легенду (в HTML разметке):
```javascript
<div className="legend-item">
    <div className="legend-color genre-western"></div>
    <span>Вестерн</span>
</div>
```

---

## Поддержка и вопросы

Если у вас возникли вопросы или проблемы при добавлении контента, проверьте:
- Правильность JSON-синтаксиса (запятые, кавычки, скобки)
- Уникальность ID
- Соответствие типов данных (числа без кавычек, строки в кавычках)
- Наличие фильмов для каждого добавленного запроса

**Удачи в создании контента для вашего ретро-магазина! 🎬📼**